const { EmbedBuilder } = require('discord.js');

module.exports = {
    name: "test",
    type: 3,
    run: async (client, interaction, config, db) => {
        return interaction.reply("this weirdo right clicked on someone, pressed apps and pressed test like a fucking weird what a weird weird weirdrwierfiwefiwefiweifweifiwefiweifiwefiwefiwei")
    },
};
